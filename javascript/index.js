function startGame() {
    const ten = document.getElementById("playerName").value.trim();
    if (ten === "") {
        alert("Chưa nhập tên");
        return;
    }
window.location.replace('url')
}